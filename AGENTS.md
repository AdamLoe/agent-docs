# Agent Router

This file is intentionally a router only. Do not add architecture, decisions,
workflow facts, status notes, or project-specific instructions here.

Start with:

- `docs/index.md`
- `docs/overview.md`

Then load the smallest matching route for the task. Durable facts belong in
`docs/`; workflow rules and reusable skills belong under `v1/`.
