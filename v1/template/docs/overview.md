# Overview

This file should stay to one screen. It gives a fresh agent enough shape to
choose the next route from `docs/index.md`.

## What this is

Describe the repo's purpose, primary runtime, and main user-facing surface.

## Major parts

Name the few durable subsystems that matter for navigation. Route detail to
`docs/architecture/`, rationale to `docs/decisions/`, and procedures to
`docs/agent-context/`.

## Hard-to-grep facts

The app binds to agent-docs through `_meta/manifest.md`; concept ownership is
structured data in `_meta/ownership.json`.
